# ==========================================
# POS Restaurante — Frontend (Vite)
# ==========================================
# En desarrollo no necesitas este archivo (usa localhost por defecto).
# En producción (Vercel), se configura como Environment Variable.

# URL del backend API (sin /api al final, eso lo agrega apiClient)
VITE_API_URL=https://tu-backend.onrender.com/api
