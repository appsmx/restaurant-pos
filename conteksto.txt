📍 Contexto POS Restaurante — Estado Real

QUÉ ES ESTO
Sistema de Punto de Venta (POS) para restaurantes, construido desde cero con React + Express + PostgreSQL + Prisma. Nombre pendiente.

QUÉ HEMOS LOGRADO
✅ Monorepo configurado y funcionando.
✅ Paquete @pos/shared compilando sin errores.
✅ Paquete @pos/api operativo. Usamos tsx watch.
✅ Base de datos restaurant_pos operativa (schema.prisma migrado, seed.ts ejecutado).
✅ Backend LEVANTADO Y ESTABLE: pnpm -F @pos/api dev corre en http://localhost:3001 sin errores.
✅ MÓDULOS DEL BACKEND CREADOS:

Auth: Login, Logout, Middleware de seguridad.
Menú: Categorías y Productos.
Floor Plan (Mesas): Secciones, Mesas, Cambio de estado.
Órdenes: Crear orden, agregar items, enviar a cocina.
✅ FRONTEND COMPLETO (BASE VISUAL):
Instalado zustand para manejo de estado.
apiClient.ts CORREGIDO.
VITE CONECTANDO CORRECTAMENTE: Configurado con host: 'localhost'.
Login funcional (pages/Login.tsx).
Layout del POS creado (components/POSLayout.tsx y Sidebar.tsx).
FloorPlan 100% FUNCIONAL: Conectado a GET /floorplan/sections. Muestra mesas por sección con colores.
MenuBrowser 100% FUNCIONAL: Conectado a GET /menu/categories. Muestra sidebar de categorías y grid de productos.
OrderPanel 100% FUNCIONAL: Conectado a GET /orders/active. Muestra tarjetas de órdenes con colores según estado. Se refresca cada 30 segundos.
✅ GIT/GITHUB: Repo subido y actualizado con los últimos cambios (FloorPlan y MenuBrowser).
EL PROBLEMA ACTUAL
Ninguno a nivel de código. El sistema es estable. (Nota de entorno: Se intentó configurar agentes de IA gratuitos como Cline+Groq y Cline+Gemini 2.5 Flash para automatizar código, pero fallaron por límites de tokens de Groq y errores 404 en la API de Google. Quedó pendiente resolver la configuración de Cline con Gemini en el futuro).

LO QUE HAY QUE HACER (en orden)
Inmediato (Lógica de negocio Frontend):

Hacer que al hacer clic en un producto del MenuBrowser, este se agregue a una orden activa (usando POST /orders y POST /orders/:id/items).
Poder enviar la orden a cocina (PATCH /orders/:id/send) desde el OrderPanel.
Después (Backend extra): Módulo de Pagos y Cerrar cuenta.
Después (Deploy): Neon (DB) + Render (API) + Vercel (Web).
ARCHIVOS DEL PROYECTO
text

C:\Users\juliq\Desktop\restaurant-pos
├── packages/
│   ├── shared/ ✅
│   └── api/
│       ├── prisma/ (schema, seed, migrations) ✅
│       └── src/ (index.ts, lib, middleware, services, routes) ✅
│
└── apps/web/
    ├── src/
│   ├── main.tsx ✅
│   ├── App.tsx ✅
│   ├── components/
│   │   ├── POSLayout.tsx ✅
│   │   └── Sidebar.tsx ✅
│   ├── pages/
│   │   ├── Login.tsx ✅
│   │   ├── FloorPlan.tsx ✅ (CONECTADO)
│   │   ├── MenuBrowser.tsx ✅ (CONECTADO)
│   │   └── OrderPanel.tsx ✅ (CONECTADO)
│   ├── stores/authStore.ts ✅
│   ├── lib/apiClient.ts ✅
│   └── styles/globals.css ✅
└── (configs de vite, tailwind, tsconfig) ✅
DETALLES TÉCNICOS CLAVE
EJECUCIÓN: Se requieren 2 terminales: 1) pnpm -F @pos/api dev 2) pnpm -F @pos/web dev.
RUTAS DEL BACKEND: /floorplan, /menu, /orders, /auth (SIN guiones).
COMANDOS dev vs build: dev corre en localhost solo para el programador. Para publicar en internet se usa build (frontend) y plataformas como Vercel/Render.
CREDENCIALES DE PRUEBA
Usuario: admin / Contraseña: Admin1234 / Rol: Admin