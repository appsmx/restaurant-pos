📍 Contexto POS Restaurante — Estado Real
(Se actualiza cada 5 cambios o cuando hay un bloqueo crítico para que cualquier IA nueva entienda el proyecto)

QUÉ ES ESTO
Sistema de Punto de Venta (POS) para restaurantes, construido desde cero con React + Express + PostgreSQL + Prisma. Nombre pendiente.

QUÉ HEMOS LOGRADO
✅ Monorepo configurado y funcionando.
✅ Paquete @pos/shared compilando sin errores.
✅ Paquete @pos/api operativo. Usamos tsx watch.
✅ Base de datos restaurant_pos operativa (schema.prisma migrado, seed.ts ejecutado).
✅ Backend LEVANTADO Y ESTABLE: pnpm -F @pos/api dev corre en http://localhost:3001 sin errores.
✅ MÓDULOS DEL BACKEND CREADOS:

Auth: Login, Logout, Middleware de seguridad.
Menú: Categorías y Productos.
Floor Plan (Mesas): Secciones, Mesas, Cambio de estado.
Órdenes: Crear orden, agregar items, enviar a cocina.
✅ FRONTEND (BASE + UI VISUAL):
Instalado zustand para manejo de estado.
Creado lib/apiClient.ts para conectar con backend.
Creado stores/authStore.ts para login real.
VITE CONECTANDO CORRECTAMENTE en http://localhost:5173.
Login funcional (pages/Login.tsx).
Layout del POS creado (components/POSLayout.tsx y Sidebar.tsx).
Páginas placeholder creadas: FloorPlan.tsx, MenuBrowser.tsx, OrderPanel.tsx (sin conectar al backend aún).
✅ GIT: Proyecto inicializado. Repo remoto creado en GitHub (appsmx/restaurant-pos). PERO NO SE HA PODIDO SUBIR NADA TODAVÍA.
EL PROBLEMA ACTUAL (BLOQUEO CRÍTICO)
Git no puede hacer el primer commit/push.
Hay carpetas "basura" corruptas anidadas dentro del proyecto (ej: packages/shared/src/packages/api/src/routes/...) que se generaron por error en sesiones pasadas. Los paths superan los 260 caracteres.

Windows no puede borrarlas con rmdir normal.
Git hace git add . pero falla con error: unable to index file... Filename too long.
Intento de fix actual: Se agregaron esas rutas al .gitignore para que Git las ignore, pero falta verificar si esto permite hacer el commit finalmente.
LO QUE HAY QUE HACER (en orden)
Inmediato (Git):

Verificar si el .gitignore actual burla a Git y permite ejecutar git add ., git commit y git push -u origin main.
Si sigue fallando, la alternativa nuclear es habilitar LongPathsEnabled en el registro de Windows (requiere reiniciar PC) o usar una herramienta como LongPathTool para borrar las carpetas corruptas.
Después (Frontend UI): Conectar los placeholders con el backend real (Traer mesas, traer menú, ver órdenes).
Después (Backend extra): Módulo de Pagos y Inventario.
Después (Deploy): Terminar de subir a GitHub, Vercel/Render.
ARCHIVOS DEL PROYECTO
text

C:\Users\juliq\Desktop\restaurant-pos
├── packages/
│   ├── shared/ ✅
│   └── api/
│       ├── prisma/ (schema, seed, migrations) ✅
│       └── src/ (index.ts, lib, middleware, services, routes) ✅
│
└── apps/web/
    ├── src/
│   ├── main.tsx ✅
│   ├── App.tsx ✅ (Maneja Login vs POSLayout)
│   ├── components/
│   │   ├── POSLayout.tsx ✅
│   │   └── Sidebar.tsx ✅
│   ├── pages/
│   │   ├── Login.tsx ✅
│   │   ├── FloorPlan.tsx ✅ (Placeholder)
│   │   ├── MenuBrowser.tsx ✅ (Placeholder)
│   │   └── OrderPanel.tsx ✅ (Placeholder)
│   ├── stores/authStore.ts ✅
│   ├── lib/apiClient.ts ✅
│   └── styles/globals.css ✅
└── (configs de vite, tailwind, tsconfig) ✅
Nota: Existen carpetas basura ocultas que no deben tocarse hasta resolver el tema de Git.

DETALLES TÉCNICOS CLAVE
vite.config.ts: DEBE usar host: 'localhost'. Si se usa '0.0.0.0', Windows Firewall bloquea la conexión y da ERR_CONNECTION_REFUSED.
conteksto.txt: Archivo del usuario en la raíz. NO BORRAR.
.gitignore: Contiene reglas añadidas recientemente para ignorar packages/api/prisma/packages/, packages/shared/packages/, etc.
CREDENCIALES DE PRUEBA
Usuario: admin / Contraseña: Admin1234 / Rol: Admin