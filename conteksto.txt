📍 Contexto POS Restaurante — Estado Real
(Se actualiza cada 5 cambios o bloqueos críticos para que cualquier IA nueva entienda el proyecto)

QUÉ ES ESTO
Sistema de Punto de Venta (POS) para restaurantes, construido desde cero con React + Express + PostgreSQL + Prisma. Nombre pendiente.

QUÉ HEMOS LOGRADO
✅ Monorepo configurado y funcionando.
✅ Paquete @pos/shared compilando sin errores.
✅ Paquete @pos/api operativo. Usamos tsx watch.
✅ Base de datos restaurant_pos operativa (schema.prisma migrado, seed.ts ejecutado).
✅ Backend LEVANTADO Y ESTABLE: pnpm -F @pos/api dev corre en http://localhost:3001 sin errores.
✅ MÓDULOS DEL BACKEND CREADOS:

Auth: Login, Logout, Middleware de seguridad.
Menú: Categorías y Productos.
Floor Plan (Mesas): Secciones, Mesas, Cambio de estado.
Órdenes: Crear orden, agregar items, enviar a cocina.
✅ FRONTEND (BASE + UI VISUAL):
Instalado zustand para manejo de estado.
apiClient.ts CORREGIDO: Sintaxis de Authorization: Bearer ${token} arreglada.
Creado stores/authStore.ts para login real.
VITE CONECTANDO CORRECTAMENTE: Configurado con host: 'localhost' en vite.config.ts.
Login funcional (pages/Login.tsx).
Layout del POS creado (components/POSLayout.tsx y Sidebar.tsx).
FloorPlan 100% FUNCIONAL: Conectado al backend (GET /floorplan/sections). Muestra mesas por sección con colores según estado.
Páginas placeholder creadas: MenuBrowser.tsx, OrderPanel.tsx (sin conectar al backend aún).
✅ GIT/GITHUB: Repo subido correctamente (appsmx/restaurant-pos).
EL PROBLEMA ACTUAL (BLOQUEO ACTUAL)
Fallo al intentar configurar un Agente de IA gratuito (Roo Code o Cline) usando la API de Groq para automatizar la escritura de código.

Se configuró Roo Code con el proveedor "OpenAI Compatible" apuntando a Groq.
El agente se queda en estado "cargando infinito" sin dar error ni respuesta.
Se intentó probar la clave de Groq mediante curl en la terminal de Windows PowerShell, pero falló porque PowerShell convierte curl en Invoke-WebRequest y la sintaxis no es compatible.
⚠️ ALERTA DE SEGURIDAD: Una API Key de Groq fue expuesta accidentalmente en el chat. HAY QUE REVOCARLA INMEDIATAMENTE en https://console.groq.com/keys y generar una nueva antes de continuar.
LO QUE HAY QUE HACER (en orden)
Inmediato (Seguridad y Agente):

Revocar la API Key de Groq expuesta y crear una nueva.
Probar conectar el agente (preferentemente Cline, ya que tiene a Groq de forma nativa en lugar de "OpenAI Compatible") con la nueva clave.
Si el agente sigue sin conectar, revisar el Firewall de Windows Saliente o probar desde otra red.
Después (Frontend UI):
Conectar MenuBrowser.tsx al backend.
Conectar OrderPanel.tsx al backend.
Después (Backend extra): Módulo de Pagos y Inventario.
Después (Deploy): Vercel/Render.
ARCHIVOS DEL PROYECTO
text

C:\Users\juliq\Desktop\restaurant-pos
├── packages/
│   ├── shared/ ✅
│   └── api/
│       ├── prisma/ (schema, seed, migrations) ✅
│       └── src/ (index.ts, lib, middleware, services, routes) ✅
│
└── apps/web/
    ├── src/
│   ├── main.tsx ✅
│   ├── App.tsx ✅
│   ├── components/
│   │   ├── POSLayout.tsx ✅
│   │   └── Sidebar.tsx ✅
│   ├── pages/
│   │   ├── Login.tsx ✅
│   │   ├── FloorPlan.tsx ✅ (CONECTADO Y FUNCIONAL)
│   │   ├── MenuBrowser.tsx ✅ (Placeholder)
│   │   └── OrderPanel.tsx ✅ (Placeholder)
│   ├── stores/authStore.ts ✅
│   ├── lib/apiClient.ts ✅
│   └── styles/globals.css ✅
└── (configs de vite, tailwind, tsconfig) ✅
DETALLES TÉCNICOS CLAVE
vite.config.ts: DEBE usar host: 'localhost'.
apiClient.ts: DEBE usar template literals: `Bearer ${token}`.
RUTAS DEL BACKEND: /floorplan (SIN guión), /menu, /orders, /auth.
CONFIGURACIÓN DEL AGENTE (Groq):
Proveedor en Cline: "Groq" (Nativo).
Proveedor en Roo: "OpenAI Compatible".
Base URL: https://api.groq.com/openai/v1
Model ID: llama-3.3-70b-versatile
POWERHELL: El comando curl no funciona igual que en Linux. Usa Invoke-WebRequest por debajo y la sintaxis -H falla.
CREDENCIALES DE PRUEBA
Usuario: admin / Contraseña: Admin1234 / Rol: Admin